{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window plus scoped SQLite access and the image file picker. The actual image copy is a narrow custom command (attach_image_file), so no broad filesystem plugin scope is granted to the webview.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "sql:default",
    "sql:allow-load",
    "sql:allow-execute",
    "sql:allow-select",
    "sql:allow-close",
    "dialog:allow-open",
    "updater:default",
    "process:allow-restart"
  ]
}
