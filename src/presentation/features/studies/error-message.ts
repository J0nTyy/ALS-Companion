// Re-exported from the shared presentation helper so existing imports keep
// working. The canonical implementation lives in `@/presentation/lib`.
export { toUserMessage } from "@/presentation/lib/error-message";
